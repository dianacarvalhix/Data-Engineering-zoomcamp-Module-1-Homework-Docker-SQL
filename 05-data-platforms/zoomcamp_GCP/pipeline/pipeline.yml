name: nyc_taxi_GCP
schedule: daily
start_date: 2022-01-01
default_connections:
  google_cloud_platform: gcp-default
  bigquery: gcp-default
variables:
  taxi_types:
    type: array
    items:
      type: string
    default: ["yellow"]
